# v0.1.0 Public-Core Candidate Verification

Date: 2026-07-27

## Decision

The local `v0.1.0` candidate satisfies the Task 8 acceptance contract. No
production change was required. This evidence authorizes local review only; it
does not authorize a remote, push, tag, repository, GitHub Release, or personal
Skill installation.

## Acceptance and full suite

The acceptance test maps the required default-branch intent, Skill validation,
public boundary, Eval count, optional `build-rwe-sap`, public TMUCRD snapshot,
and repository-policy criteria to real Git repositories, the real validators,
and source files. Default-branch intent is detached-checkout compatible: a
local `refs/heads/main` is accepted first, followed by GitHub PR
`GITHUB_BASE_REF` or push `GITHUB_REF` metadata. A non-main target is rejected.

| Command | Result |
|---|---|
| `/tmp/clin-data-nav-venv/bin/python -m pytest -q` | PASS: 93 passed in 0.36s |

The original candidate's first complete-suite run was GREEN at 89 tests.
Review round 1 added four real-Git test cases without modifying production
Skill package source.

### Detached-checkout CI RED/GREEN evidence

1. RED: the local-main-ref test initialized and committed a real `main`
   repository, detached `HEAD`, and failed because
   `default_branch_is_main()` did not accept a repository path.
2. GREEN: after checking the explicit local `refs/heads/main`, the focused
   local-ref and candidate-contract run passed: `2 passed in 0.04s`.
3. RED: detached real repositories without a local `main` failed both trusted
   GitHub fallback cases while the local-ref and non-main cases passed:
   `2 failed, 2 passed in 0.07s`.
4. GREEN: after the ordered GitHub metadata fallback was added, all focused
   detached-checkout cases passed: `4 passed, 1 deselected in 0.06s`.
5. Full GREEN: the complete suite passed: `93 passed in 0.36s`.

## Command-line gates

| Command | Exit | Warnings or findings |
|---|---:|---|
| `/tmp/clin-data-nav-venv/bin/python scripts/validate_skill.py` | 0 | None |
| `/tmp/clin-data-nav-venv/bin/python scripts/check_public_boundary.py` | 0 | None |
| `/tmp/clin-data-nav-venv/bin/python scripts/package_skill.py --check-reproducible` | 0 | None |

## Repository audit

- `git branch --show-current` reported `main` for the local candidate. The
  acceptance helper no longer requires an attached checkout in CI.
- Before this report was created, `git status --short` contained only the new
  `tests/test_acceptance.py`.
- `git log --oneline --decorate` contained 19 local commits from the approved
  design through the Task 7 packaging-gate clarification; the inspected tip
  was `7f1f343`.
- `git ls-files` contained 50 tracked paths and no tracked private artifact.
- The required blocked-filename grep produced only policy, enforcement,
  synthetic fixture, Eval, and test references. It found no tracked private
  material.
- The Skill ID remains `clinical-data-research-navigator` in frontmatter,
  metadata, validator, packaging, installer, tests, and documentation.
- Outside the approved design and plan, a search for repository-name-like
  alternatives returned no matches. The repository name remains
  `clin-data-nav`.

The following release-critical files were opened and reviewed:

- `skills/clinical-data-research-navigator/SKILL.md`
- `skills/clinical-data-research-navigator/agents/openai.yaml`
- `skills/clinical-data-research-navigator/references/tmucrd-public-profile.md`
- `skills/clinical-data-research-navigator/references/institutional-adapter-contract.md`
- `evals/cases.yaml`
- `AGENTS.md`
- `.github/workflows/validate.yml`

## Local candidate

The candidate was built once with:

```text
/tmp/clin-data-nav-venv/bin/python scripts/package_skill.py --output-dir dist
```

| File | Bytes | SHA-256 |
|---|---:|---|
| `dist/clinical-data-research-navigator-0.1.0.zip` | 9366 | `44f0aa3a0e51a1878ce4b7fd2fea64493d1fb6d55b33eb5b64d722a26142c0ac` |
| `dist/clinical-data-research-navigator-0.1.0.manifest.json` | 984 | `86474027c0d66554adb4baa648f7452985ea391ed6f8c9caa5a34cda24776e8a` |

The manifest records the same archive SHA-256. Both files are matched by the
repository's `dist/` ignore rule and remain local review artifacts. Review
round 1 changed only acceptance tests and this evidence, so the Skill package
filenames, byte sizes, and hashes remained unchanged.

## Publication boundary

Before the evidence commit:

- `git remote -v` returned no remotes;
- `git tag --list v0.1.0` returned no tag;
- no GitHub repository or Release was created;
- no personal Skill installation was attempted.

Final clean-tree, remote, tag, and ignored-artifact checks are required after
the evidence commit.

## Deferred whole-branch review items

These prior-ledger items did not fail Task 8 acceptance and were not changed:

- evaluator invalid-`unicode_form` continuation;
- installer `overwrite=False` TOCTOU;
- CI offline wording;
- exact forbidden-regex breadth;
- validator invalid YAML handling.

They remain concerns for the final whole-branch review, not Task 8 fixes.
