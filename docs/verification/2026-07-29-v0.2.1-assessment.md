# v0.2.1 Assessment Revalidation

Date: 2026-07-29

## Decision

The pushed `v0.2.2` remediation candidate fixes the reproducible public-build
and documentation findings confirmed against the published `v0.2.1`
baseline. Fresh editable installation and all four acceptance commands pass
on disposable Windows and Linux Python 3.11 environments. GitHub-hosted
Ubuntu and Windows jobs also passed the same four-command set for exact commit
`a0704b2416be169abe17903215e8249ec6892ba0`.

GitHub registered both validation and Release workflows at the candidate
commit. The Release workflow was not dispatched, and no tag or GitHub Release
was created or modified. GitHub reports `main` as `protected: false`; the
branch-protection endpoint returns HTTP 404 with `Branch not protected`.
Required checks are therefore not enforced through branch protection. No
repository setting was changed.

## Baseline

- Published tag: annotated `v0.2.1`
- Tag object: `06c386b2ceae435a0b2bd9a43ed3f33a3177a110`
- Tagged commit: `047eb61306b02c9f20eab762881fa28aefa93647`
- Confirmed failing run:
  [GitHub Actions run 30416612689](https://github.com/mtchuang1981/clin-data-nav/actions/runs/30416612689)
- Local acceptance candidate before this report:
  `549f8f6a4cfef9ae640d717aab40691a278f43f2`
- Pushed and GitHub-hosted validated candidate:
  `a0704b2416be169abe17903215e8249ec6892ba0`

The baseline failure occurred during `pip install -e ".[dev]"`, before the
test and validation gates could run. The package boundary is now explicitly
declared in `pyproject.toml`.

## Finding disposition

The status vocabulary in this table is intentionally limited to the four
approved assessment values.

| Area | Revalidation result | Status |
|---|---|---|
| Editable install | A new Windows Python 3.11 venv and a clean Linux Python 3.11 container both installed `.[dev]` successfully. | Confirmed and fixed |
| Windows platform tests | Native Windows and a disposable Windows Python 3.11 environment passed all 214 tests; the installer platform seam retained atomic no-overwrite behavior. | Confirmed and fixed |
| Linux/Windows CI | Exact pushed commit `a0704b2416be169abe17903215e8249ec6892ba0` passed the GitHub-hosted Ubuntu and Windows matrix; all four verification steps completed successfully in both jobs. | Confirmed and fixed |
| Four gates | Pytest, Skill validation, public-boundary scanning, and reproducible packaging passed locally on native Windows, fresh Windows 3.11, and clean Linux 3.11. | Confirmed and fixed |
| Release gate | Local tests and workflow contracts enforce an annotated version-matched tag, immutable verified commit and tag identity, dual-platform validation, reproducible assets, and fail-closed publication. GitHub registered `release.yml` as active; it was not dispatched and no publication action was taken. | Confirmed and fixed |
| README onboarding | Both READMEs now define Node/npm prerequisites, terminal-versus-Codex command boundaries, project-local install/update commands, and a 60-second first-success path. | Confirmed and fixed |
| Eval scope | Documentation now distinguishes the 11-case catalog from the three scored fixture pairs and does not overstate clinical coverage. | Confirmed and fixed |
| Unrelated local configuration | The tracked `.baoyu-skills` state was removed; policy tests allow untracked local state but reject it at the tracked public boundary. | Confirmed and fixed |
| Version synchronization | Project metadata, citation, packager, installer, READMEs, changelogs, Release notes, ZIP, and manifest use `0.2.2`; historical records remain unchanged. | Confirmed and fixed |
| Branch protection and repository topics | GitHub reports `main` as `protected: false`; the protection endpoint returns HTTP 404 with `Branch not protected`. No setting was changed. Repository topics were not changed. | Remaining P2 |
| Citation author enrichment and optional Skill icon/color | Richer author identity such as ORCID and optional UI metadata remain outside this patch. | Remaining P2 |
| Next behavior and information-architecture phase | Four output depths, broader beginner glossary/navigation, and additional scored Eval fixtures require a separate design and validation cycle. | Deferred to v0.3.0 |

## GitHub-hosted validation evidence

GitHub Actions validated exact commit
`a0704b2416be169abe17903215e8249ec6892ba0` in
[run 30460947358](https://github.com/mtchuang1981/clin-data-nav/actions/runs/30460947358).
The push-triggered `validate` workflow completed successfully:

| Job | Job ID | Conclusion | Four verification steps |
|---|---:|---|---|
| `test (windows-latest)` | `90606546030` | `success` | pytest, Skill validation, public-boundary scan, and reproducible-package check all completed with `success` |
| `test (ubuntu-latest)` | `90606546032` | `success` | pytest, Skill validation, public-boundary scan, and reproducible-package check all completed with `success` |

The GitHub Actions API reported both `.github/workflows/validate.yml`
(workflow ID `322119575`) and `.github/workflows/release.yml` (workflow ID
`323030503`) as `active`. The Release workflow was not dispatched.

A read-only request for
`repos/mtchuang1981/clin-data-nav/branches/main` reported `protected: false`
at commit `13574e2101378f4d8c426f1819d7e50a5c1d413a`. The corresponding
`branches/main/protection` endpoint returned HTTP 404 with `Branch not
protected`. No repository setting was changed.

## Native Windows evidence

The installed host runtime was Python `3.13.13`. The required commands ran
from the isolated worktree:

| Command | Exit | Result |
|---|---:|---|
| `python -m pytest -q` | 0 | `214 passed in 14.14s` |
| `python scripts/validate_skill.py` | 0 | no findings |
| `python scripts/check_public_boundary.py` | 0 | no findings |
| `python scripts/package_skill.py --check-reproducible` | 0 | reproducibility confirmed |

## Fresh Windows Python 3.11 evidence

The host did not have Python 3.11 installed. A Python.org installer was
downloaded from
`https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe` and checked
before execution:

| Property | Verified value |
|---|---|
| Authenticode status | `Valid` |
| Signer | `CN=Python Software Foundation, O=Python Software Foundation, L=Beaverton, S=Oregon, C=US` |
| Installer SHA-256 | `5ee42c4eee1e6b4464bb23722f90b45303f79442df63083f05322f1785f5fdde` |
| Runtime | `Python 3.11.9` 64-bit |

The installer used a GUID-named direct child of the Windows temporary
directory with `InstallAllUsers=0`, `Include_launcher=0`,
`InstallLauncherAllUsers=0`, `AssociateFiles=0`, `PrependPath=0`,
`AppendPath=0`, and `Shortcuts=0`. A separate GUID-named venv then ran:

| Command | Exit | Result |
|---|---:|---|
| `python -m pip install --upgrade pip` | 0 | pip upgraded in the disposable venv |
| `python -m pip install -e ".[dev]"` | 0 | editable install succeeded |
| `python -m pytest -q` | 0 | `214 passed, 1 warning in 13.77s` |
| `python scripts/validate_skill.py` | 0 | no findings |
| `python scripts/check_public_boundary.py` | 0 | no findings |
| `python scripts/package_skill.py --check-reproducible` | 0 | reproducibility confirmed |

The warning was a `PytestCacheWarning`: the elevated validation identity could
not write the repository `.pytest_cache` owned by the sandbox identity. The
test temporary root was isolated to a new GUID directory; no repository ACL
or test behavior was changed.

After validation, the venv path was resolved and checked to have the Windows
temporary directory as its exact parent and the expected
`clin-data-nav-acceptance-` prefix. Python 3.11.9 was then silently
uninstalled. Every remaining installer, installation, venv, and isolated-test
temporary directory was checked by exact parent and fixed prefix before
removal. `PATH` was unchanged, and `py -0p` again listed only the original
Python 3.13 runtime.

## Clean Linux Python 3.11 evidence

The final run used:

- image: `python:3.11-slim`;
- repo digest:
  `python@sha256:db3ff2e1800a8581e2c48a27c3995339d47bdf046da21c7627accd3d51053a93`;
- image ID:
  `sha256:1c03896dbf0ef4a2b86f1462395249e810c589e25d8c0b002faabc38aa144686`;
- runtime: `Python 3.11.15`;
- Debian Git: `2.47.3`.

The bare slim image does not include Git, while 32 repository tests exercise
real Git repositories. The exact first container command therefore produced
`182 passed, 32 failed, 2 warnings in 5.33s`; all 32 failures had the same
root cause, `FileNotFoundError: git`. After the approved minimal installation
of Debian's `--no-install-recommends git`, the direct Windows worktree mount
produced `213 passed, 1 failed, 2 warnings in 5.93s`. Its only failure was the
acceptance check because the worktree `.git` file contains a Windows `E:/...`
gitdir that Linux cannot resolve.

The corrected clean-room run did not copy untracked files or reuse Git
environment redirects. It:

1. created `candidate.tar` with `git archive HEAD`, so only tracked candidate
   files were included;
2. mounted that archive read-only in a disposable `python:3.11-slim`
   container;
3. installed Debian's minimal Git package;
4. extracted the archive into the container, initialized `main`, and created
   one synthetic commit;
5. ran the same editable install and four-command acceptance set.

The tracked archive SHA-256 was
`24241cf9ffaba3d5b0e2a84ddc649f8f0b936135e84bd051c3575964914dce6d`.
The final results were:

| Command | Exit | Result |
|---|---:|---|
| `python -m pip install --upgrade pip` | 0 | pip upgraded in the disposable container |
| `python -m pip install -e ".[dev]"` | 0 | editable install succeeded |
| `python -m pytest -q` | 0 | `214 passed in 1.13s` |
| `python scripts/validate_skill.py` | 0 | no findings |
| `python scripts/check_public_boundary.py` | 0 | no findings |
| `python scripts/package_skill.py --check-reproducible` | 0 | reproducibility confirmed |

The container and tracked-source archive were removed after the run. The two
earlier failures are retained here because they document environment
preconditions rather than being rewritten as successful evidence.

## Candidate artifacts

The ignored `dist/` directory was built locally, then checked by the
independent release verifier and by PowerShell:

```text
python scripts/package_skill.py --output-dir dist
python scripts/verify_release.py artifacts --archive dist/clinical-data-research-navigator-0.2.2.zip --manifest dist/clinical-data-research-navigator-0.2.2.manifest.json
release artifacts verified
```

| Artifact property | Verified value |
|---|---|
| ZIP | `dist/clinical-data-research-navigator-0.2.2.zip` |
| ZIP bytes | `14641` |
| ZIP SHA-256 | `f67b921cbbe71ff6eef56fec628cc72c42cc220b01b5f34940116e1628268667` |
| Manifest | `dist/clinical-data-research-navigator-0.2.2.manifest.json` |
| Manifest bytes | `1118` |
| Manifest SHA-256 | `73c77ccb35eb051c8139a0aef532bbb11e26b0675bd1e69eddd77b21b6f4819a` |
| Manifest `archive_sha256` | `f67b921cbbe71ff6eef56fec628cc72c42cc220b01b5f34940116e1628268667` |
| Public file count | `7` |

PowerShell lowercased the independent `Get-FileHash` result and compared it
with the manifest field; they matched exactly. The ZIP and manifest remain
ignored local artifacts and are not part of the evidence commit.

## Remaining external and design work

Task 11 proved the exact pushed candidate on both GitHub-hosted matrix jobs and
confirmed that `validate.yml` and `release.yml` are active. GitHub reports
`main` as unprotected (`protected: false`; protection endpoint HTTP 404,
`Branch not protected`). No setting was changed.

GitHub topics, branch-protection policy, richer `CITATION.cff` author identity,
and optional Skill icon/color remain P2 or external settings. The recommended
`v0.3.0` design scope is the four output-depth model, broader beginner
glossary and navigation, and more scored Eval fixtures. Those items must not
be inferred from the three current scored fixture pairs.
