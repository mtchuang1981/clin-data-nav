# v0.3.0 Public Publication Evidence

- **Evidence recorded:** 2026-08-09 (`Asia/Taipei`)
- **Published version:** `v0.3.0`
- **Published main commit:**
  `6cf9593dd8a520f56e1e6e5b0bf2cb7d40b97791`
- **Public Release:**
  `https://github.com/mtchuang1981/clin-data-nav/releases/tag/v0.3.0`

This is a dated post-publication record. It does not amend the earlier local
candidate evidence in `2026-08-02-v0.3.0-local.md`, move the published tag,
edit the GitHub Release, or claim that this later evidence commit is contained
in the immutable v0.3.0 source tree.

## Local release preparation

The final release preparation used development Python 3.13.13 and a temporary
official Python.org 3.11.9 64-bit installation. The installer had SHA-256
`5ee42c4eee1e6b4464bb23722f90b45303f79442df63083f05322f1785f5fdde`,
a valid Authenticode signature from the Python Software Foundation, and made
no PATH, launcher, file-association, or shortcut change.

Both interpreters passed all 280 tests and the four required repository gates:

```text
python -m pytest -q
python scripts/validate_skill.py
python scripts/check_public_boundary.py
python scripts/package_skill.py --check-reproducible
```

The Python 3.11 run emitted one `PytestCacheWarning` because the isolated
identity could not write the repository's existing `.pytest_cache`; all 280
tests passed, product files were unchanged, and the temporary runtime was
uninstalled and removed after verification.

## Hosted validation

The pushed release commit ran the Ubuntu, Windows, and cross-runner package
comparison jobs successfully:

- run ID: `31263843137`;
- URL:
  `https://github.com/mtchuang1981/clin-data-nav/actions/runs/31263843137`;
- head SHA: `6cf9593dd8a520f56e1e6e5b0bf2cb7d40b97791`;
- conclusion: `success`; and
- check annotations: zero, including zero Node.js 20 or deprecation
  annotations.

The `compare-packages` job confirmed that the Ubuntu and Windows ZIP and
manifest candidates were byte-identical.

## Annotated tag identity

The new annotated tag was verified before and after publication:

| Identity | SHA |
|---|---|
| Tag object | `87ca8f379f6751fc465dbcd6ae8f430dabc73523` |
| Peeled commit | `6cf9593dd8a520f56e1e6e5b0bf2cb7d40b97791` |

The tag object type was `tag`. No existing tag was moved or replaced.

## Guarded Release publication

The manually dispatched guarded workflow completed successfully:

- run ID: `31263961241`;
- URL:
  `https://github.com/mtchuang1981/clin-data-nav/actions/runs/31263961241`;
- head SHA: `6cf9593dd8a520f56e1e6e5b0bf2cb7d40b97791`;
- conclusion: `success`; and
- public Release:
  `https://github.com/mtchuang1981/clin-data-nav/releases/tag/v0.3.0`.

The Release was published on 2026-08-08 at 15:16:25 UTC. It is neither a draft
nor a prerelease. The publish job rechecked the remote tag object and peeled
commit before creating the Release and could not edit an existing Release.

## Public assets

| Asset | GitHub asset ID | Size (bytes) | SHA-256 |
|---|---:|---:|---|
| `clinical-data-research-navigator-0.3.0.zip` | `506494592` | `18591` | `ce6a67a268e4266d094db31406a9c5dda3f005c3b6a5355ec851ea87abf3aded` |
| `clinical-data-research-navigator-0.3.0.manifest.json` | `506494593` | `1265` | `d3862e00fcf499fa453e6cac05b6f3e21f9b2a9d735d2579a4a5b834caee42bc` |

Direct download URLs:

- `https://github.com/mtchuang1981/clin-data-nav/releases/download/v0.3.0/clinical-data-research-navigator-0.3.0.zip`
- `https://github.com/mtchuang1981/clin-data-nav/releases/download/v0.3.0/clinical-data-research-navigator-0.3.0.manifest.json`

Both assets were downloaded from the public Release into a new GUID-named
temporary directory. The following verifier passed:

```text
python scripts/verify_release.py artifacts --archive <downloaded-zip> --manifest <downloaded-manifest>
```

The manifest's `archive_sha256` equalled the downloaded ZIP hash. Both public
asset hashes also matched the locally reviewed candidate bytes, and the
temporary directory was removed after verification.

## External repository settings

A read-only re-read on 2026-08-09 (`Asia/Taipei`) found:

- branch protection: disabled;
- rulesets: none;
- topics: none;
- private vulnerability reporting: disabled; and
- Dependabot security updates: disabled.

No external repository setting was changed during this evidence re-read. A
successful workflow run does not mean its check names are required by branch
protection. The operator procedures in `docs/repository-settings.md` remain
the authoritative checklist for any separately approved future change.

## Remaining intentional boundaries

- No Zenodo deposition or DOI has been verified or claimed.
- `CITATION.cff` retains a non-invented collective contributor identity; no
  legal name, affiliation, email, or ORCID was inferred.
- No unapproved icon, color, or brand asset was added to Skill UI metadata.
- No separate Plugin product was built or published.
- The optional `build-rwe-sap` Skill remains unbundled and is never installed
  automatically.

These are deliberate product and authorization boundaries, not evidence that
the v0.3.0 publication failed.
