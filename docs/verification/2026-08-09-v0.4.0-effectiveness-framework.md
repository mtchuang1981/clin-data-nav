# v0.4.0 Effectiveness Framework Verification

- Verification date: `2026-08-09` (`Asia/Taipei`, UTC+08:00; Windows zone
  `Taipei Standard Time`)
- Observation captured: `2026-08-09T20:00:32+08:00`
- Branch: `codex/effectiveness-evaluation-design`
- Implementation HEAD: `eb61bca2c7b5cfd0955bf99e5c19eaf4c19c866d`
- Host interpreter: `Python 3.13.13`
- Target interpreter: official `Python 3.11.9` 64-bit embeddable runtime

The exact implementation object identified by this record is the Implementation
HEAD above. Before the Task 9 contract was added, that clean object produced
`507 passed in 25.52s` under the host interpreter. The evidence document and its
contract test necessarily follow the implementation object: while the record is
being written they are uncommitted Task 9 changes, so this record does not call
that working tree clean. The evidence commit will contain only those two files
and will be verified again after commit. A commit cannot truthfully embed its
own final SHA, so the evidence commit is identified in Git history and the Task
9 execution report rather than represented here as the Implementation HEAD.

## Test-driven evidence contract

The evidence contract was added to `tests/test_project_metadata.py` before this
file existed. This exact node was run:

```text
python -m pytest tests/test_project_metadata.py::test_effectiveness_framework_evidence_is_dated_and_does_not_claim_a_pilot -q
```

It exited `1` with `1 failed in 0.24s`. The failure was the expected
`FileNotFoundError` for this document, not a collection error or an unrelated
failure. A later full run before document creation likewise observed
`1 failed, 507 passed in 21.65s` on Python 3.13.13 and
`1 failed, 507 passed in 22.84s` on Python 3.11.9; in both cases the only
failure was this missing evidence file.

## Official Python 3.11.9 runtime

No Python 3.11 runtime was present in the Windows launcher, common installation
paths, or the existing `C:\tmp` cache. The official Windows embeddable package
was therefore downloaded outside the repository.

| Property | Observed value |
|---|---|
| Source URL | `https://www.python.org/ftp/python/3.11.9/python-3.11.9-embed-amd64.zip` |
| Download size | `11,249,023` bytes |
| Locally computed SHA-256 | `009D6BF7E3B2DDCA3D784FA09F90FE54336D5B60F0E0F305C37F400BF83CFD3B` |
| Runtime | `3.11.9 (tags/v3.11.9:de54cf5, Apr 2 2024, 10:12:12) [MSC v.1938 64 bit (AMD64)]` |
| Temporary executable | `C:\tmp\python-3.11.9-embed-amd64\python.exe` |

The ZIP hash above is the locally observed download hash, not a claim that it
was independently matched to a separately published checksum. The isolated
runtime used existing pure-Python `pytest 9.0.2`, `PyYAML 6.0.3`, and their
pure-Python dependencies from a separate `C:\tmp` directory. The incompatible
Python 3.13 `_yaml` binary was excluded; the target runtime reported
`yaml.__with_libyaml__ == False`. Its subprocess smoke test returned
`Python 3.11.9`. The embeddable `._pth` explicitly named only the temporary
dependency directory and this worktree so repository modules and subprocess
tests resolved normally. No installer, project dependency, declared Python
support, PATH, file association, or tracked file was changed for this runtime.

## Framework structure and focused checks

A direct in-memory contract and assignment check exited `0` and observed:

- eight offline task pairs;
- two pairs at each of the four depths: quick explanation, evidence navigation,
  implementation specification, and research design;
- 16 variants in total;
- a 16-person assignment with 64 rows across synthetic participant codes
  `B01`-`B08` and `P01`-`P08`;
- four rows for every synthetic participant code;
- 32 control and 32 intervention assignments; and
- no assignment-validator errors.

The focused command covering the public contract, assignment balance,
commitment example, ratings lock, condition key, explicit unlock, and
aggregate-only report exited `0` with `10 passed in 0.24s`:

```text
python -m pytest -q tests/test_effectiveness_contract.py::test_offline_bank_has_eight_pairs_two_per_depth_and_two_variants tests/test_study_assignments.py::test_assignment_has_sixteen_people_four_tasks_and_balanced_conditions tests/test_human_task_commitment.py::test_public_example_is_exactly_recomputed_with_zero_nonce tests/test_effectiveness_analysis.py::test_lock_requires_completed_ratings_raw_byte_hash_and_closed_schema tests/test_effectiveness_analysis.py::test_condition_key_requires_exact_unique_answer_set_and_valid_conditions tests/test_effectiveness_analysis.py::test_unlock_requires_same_study_id_as_manifest_and_among_rating_files tests/test_effectiveness_analysis.py::test_cli_requires_explicit_post_lock_unlock_without_echoing_paths tests/test_effectiveness_reports.py::test_checked_in_example_is_aggregate_only_and_explicitly_synthetic
```

The commitment check recomputed only the checked-in example from the public
offline task bank and an all-zero example nonce. It verified the example
contains neither nonce nor prompt text. It did not create an actual human task
commitment. The lock/key tests exercised rejection and explicit-unlock
boundaries without using a real condition key or human data. The checked-in
English and Traditional Chinese reports were verified as aggregate-only,
explicitly synthetic examples; they are not pilot outcomes.

## Pre-document command observations

These runs establish the inputs used to write this document. The complete
pytest command was intentionally RED because this file did not yet exist; the
remaining commands independently exited `0`.

### Host Python 3.13.13

| Command | Exit | Observed result | Wall time |
|---|---:|---|---:|
| `python -m pytest -q` | 1 | `1 failed, 507 passed in 21.65s`; only the expected missing evidence file | `22,361 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `410 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `392 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `111 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `91 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; both checked-in synthetic reports accepted | `61 ms` |
| `git diff --check main...HEAD` | 0 | no output/whitespace errors in the committed branch diff | `80 ms` |

### Target Python 3.11.9

| Command | Exit | Observed result | Wall time |
|---|---:|---|---:|
| `python -m pytest -q` | 1 | `1 failed, 507 passed in 22.84s`; only the expected missing evidence file | `23,547 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `75 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `182 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `115 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `119 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; both checked-in synthetic reports accepted | `54 ms` |

## Document-present fresh verification

After this evidence file existed and the contract node was GREEN, the complete
command set was run again from the worktree root. The working tree at this point
was deliberately not clean: `tests/test_project_metadata.py` was modified and
this evidence file was untracked. No other path was changed.

### Host Python 3.13.13

| Command | Exit | Observed result | Wall time |
|---|---:|---|---:|
| `python -m pytest -q` | 0 | `508 passed in 21.74s` | `22,825 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `61 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `185 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `98 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `83 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; both checked-in synthetic reports accepted | `56 ms` |
| `git diff --check main...HEAD` | 0 | no output/whitespace errors in the committed branch diff | `52 ms` |

### Target Python 3.11.9

For this table, the displayed plan command `python` resolved to the temporary
official 3.11.9 executable recorded above.

| Command | Exit | Observed result | Wall time |
|---|---:|---|---:|
| `python -m pytest -q` | 0 | `508 passed in 22.31s` | `23,013 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `77 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `188 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `111 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `109 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; both checked-in synthetic reports accepted | `54 ms` |

## Complete diff and public-boundary audit

The complete committed `main...HEAD` diff and the Task 9-inclusive diff against
`main` were loaded for review. They contained no binary patch. Both
`git diff --check main` and `git diff --cached --check` exited `0`. With the two
Task 9 files staged so that `git ls-files` included them, the fresh
`python scripts/check_public_boundary.py` scan exited `0` with no findings.

The changed-path review found no change to `pyproject.toml`, `.github/`,
`docs/release.md`, `docs/releases/`, `CITATION.cff`, or `CHANGELOG.md`; therefore
the branch diff contains no package-support, workflow permission, release-note,
or citation-version mutation. The only tracked effectiveness data files are the
public offline YAML, rubric, input/schema and protocol documentation, empty
report templates, an `example_only` commitment wrapper, and explicitly
synthetic aggregate summary and bilingual reports. There is no tracked
assignment file, participant-level dataset, human answer, blinded score file,
ratings lock, condition key, nonce, confidential task pack, or external model
snapshot represented as deterministic evidence.

## Evidence and authority boundary

This is a local framework verification record. There was no external model call
for this verification. There was no human recruitment or data collection, no
actual human task commitment, no real ratings lock or condition-key unlock, no
tag or settings change, and no v0.4.0 Release. No participant row, human answer,
condition key, nonce, confidential task pack, patient data, private adapter,
codingbook, data dictionary, physical schema, linkage rule, PII classification,
or version-specific institutional metadata was read or added.

The only outcome example is synthetic and aggregate-only. This record does not
claim that the Skill is effective, clinically valid, causally valid, or ready
for real-use deployment. Completing the framework commit does not authorize
governance, recruitment, human data collection, rating, unlock, analysis, or
publication activity.

Remaining external steps require separate authorization and observed inputs:
the governance and ethics-path decision; governed storage, consent, access,
retention, and incident decisions; confidential task-pack preparation; actual
nonce and commitment creation; recruitment and data collection; ratings and
data lock; condition-key unlock; aggregate reporting; and conservative power
scenarios based on the final design and observed pilot inputs.
