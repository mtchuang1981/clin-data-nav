# v0.4.0 Local Release Candidate Verification

- Verification date: `2026-08-10` (`Asia/Taipei`)
- Verified implementation HEAD:
  `7c681b04a63beecdf20f528b1380060c55a7856f`
- Branch: `codex/v0.4.0-release`
- Host runtime: `Python 3.13.13`
- Official temporary runtime: `Python 3.11.9`

This record verifies the implementation object above. The evidence commit follows
that implementation commit and therefore cannot contain its own final commit SHA.
The candidate commit is recorded after the evidence commit is created and verified.

## Evidence contract RED

The exact node
`python -m pytest tests/test_project_metadata.py::test_v040_local_release_evidence_records_verified_candidate -q -p no:cacheprovider`
exited `1` with `1 failed in 0.26s`. The expected failure was
`FileNotFoundError` for this then-missing evidence file.

## Runtime identity and repository path

The host `python --version` reported `Python 3.13.13`. The official temporary
executable at `C:\tmp\python-3.11.9-embed-amd64\python.exe` reported
`Python 3.11.9` and the full runtime string
`3.11.9 (tags/v3.11.9:de54cf5, Apr 2 2024, 10:12:12) [MSC v.1938 64 bit (AMD64)]`.

Both `C:\tmp\python-3.11.9-embed-amd64\python311._pth` and a direct `sys.path`
observation included
`E:\6GAI\AGY\clin-data-nav\.worktrees\v0.4.0-release`. The `_pth` also named
`python311.zip`, `.`, and `C:\tmp\python-3.11.9-puredeps`; `import site` remained
commented out. These observations confirm that the official runtime used this
release worktree rather than another checkout.

## Implementation verification

The evidence-only contract was temporarily absent for these implementation-HEAD
runs after its expected RED had been observed. This avoids counting a known
missing evidence file as an implementation failure. Every command below was run
from the release worktree. Elapsed values are observed wall-clock milliseconds.

### Host Python 3.13.13

| Command | Exit | Result | Elapsed |
|---|---:|---|---:|
| `python -m pytest -q -p no:cacheprovider` | 0 | `552 passed in 23.61s` | `24,590 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `68 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `192 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `99 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `96 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; checked-in synthetic bilingual reports accepted | `110 ms` |

### Official Python 3.11.9

In this table, `python` denotes the confirmed official executable
`C:\tmp\python-3.11.9-embed-amd64\python.exe`, whose observed `sys.path`
included the verified worktree
`E:\6GAI\AGY\clin-data-nav\.worktrees\v0.4.0-release`.

| Command | Exit | Result | Elapsed |
|---|---:|---|---:|
| `python -m pytest -q -p no:cacheprovider` | 0 | `552 passed in 26.17s` | `27,301 ms` |
| `python scripts/validate_skill.py` | 0 | no output/findings | `74 ms` |
| `python scripts/check_public_boundary.py` | 0 | no output/findings | `199 ms` |
| `python scripts/package_skill.py --check-reproducible` | 0 | no output; reproducibility check accepted | `125 ms` |
| `python scripts/render_eval_summary.py --check` | 0 | no output; checked-in deterministic Eval summary accepted | `125 ms` |
| `python scripts/render_effectiveness_report.py --summary evals/effectiveness/examples/synthetic-summary.json --english evals/effectiveness/examples/synthetic-report.md --traditional-chinese evals/effectiveness/examples/synthetic-report.zh-TW.md --check` | 0 | no output; checked-in synthetic bilingual reports accepted | `125 ms` |

## Local release artifacts

The existing `dist` directory was resolved inside the release worktree, removed,
and confirmed absent before generation. The host command
`python scripts/package_skill.py --output-dir dist` exited `0` in `433 ms`.
`python scripts/verify_release.py artifacts --archive dist/clinical-data-research-navigator-0.4.0.zip --manifest dist/clinical-data-research-navigator-0.4.0.manifest.json`
then exited `0` in `115 ms` and printed `release artifacts verified`.

| Property | Observed value |
|---|---|
| ZIP filename | `clinical-data-research-navigator-0.4.0.zip` |
| ZIP size | `18591` bytes |
| ZIP SHA-256 | `ce6a67a268e4266d094db31406a9c5dda3f005c3b6a5355ec851ea87abf3aded` |
| Manifest filename | `clinical-data-research-navigator-0.4.0.manifest.json` |
| Manifest size | `1265` bytes |
| Manifest SHA-256 | `9fe69bfa0a5fd9f8ce58082c1989316ed5b46683d74176319ba6c1187063a85a` |
| Manifest `archive_sha256` | `ce6a67a268e4266d094db31406a9c5dda3f005c3b6a5355ec851ea87abf3aded` |
| ZIP file count | `8` |

Independent PowerShell `Get-FileHash -Algorithm SHA256` calculations produced the
two SHA-256 values above. Independent manifest and ZIP inspection verified the
exact root schema `archive,archive_sha256,files,name,version`, version `0.4.0`,
the exact archive filename, and exact per-file schema `path,sha256,size`.
Manifest file records are sorted and unique. ZIP members exactly equal the
manifest paths. The calculated ZIP digest equals the manifest
`archive_sha256`.

The ZIP was extracted into a new GUID-named directory under this release
worktree. `python scripts/check_public_boundary.py <extracted-directory>` exited
`0` with no findings; the temporary extraction directory was then removed and
confirmed absent. The generated `dist` artifacts are ignored build outputs and
are not included in the evidence commit.

## Evidence and publication boundary

No human pilot was conducted. This evidence does not claim that the Skill is
effective. It also does not claim clinical validity, causal validity, human
outcomes, or fitness for real-world deployment. Only repository tests and
synthetic examples were evaluated; no human participant data, condition keys,
human answer text, study assignments, consent records, or private institutional
material were used.

The `v0.4.0` tag does not yet exist. The GitHub Release has not yet been
published. This Task 3 work did not push, tag, dispatch a workflow, create a
Release, or run remote checks. Local verification does not establish remote CI,
tag, Release, or public-asset state; those remain separate guarded steps.
