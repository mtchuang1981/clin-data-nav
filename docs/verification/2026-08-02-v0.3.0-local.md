# v0.3.0 Local Verification

- Verification completed: `2026-08-02T04:54:28+08:00` (`Asia/Taipei`)
- Verified implementation HEAD:
  `35e4ec5c92c9d13be16503fb5f49dffc67716f03`
- Branch: `codex/v0.3.0-new-user-experience`
- Development interpreter: `Python 3.13.13`
- Supported clean-room interpreter: `Python 3.11.9` 64-bit

The exact implementation object verified by this record is the implementation
HEAD above. The evidence-report commit is created only after the checks in this
record pass; it is a later documentation commit and is not represented as the
implementation object that generated the evidence. Its SHA is reported by Git
history and the Task 11 execution record rather than embedded here, because a
commit cannot contain its own final SHA without changing that SHA.

## Development-environment verification

All commands were run from the repository root at the verified implementation
HEAD. An empty result means the command exited successfully with no findings.

| Command | Exit | Result |
|---|---:|---|
| `python -m pytest -q` | 0 | `264 passed in 16.20s` |
| `python scripts/validate_skill.py` | 0 | no findings |
| `python scripts/check_public_boundary.py` | 0 | no findings |
| `python scripts/package_skill.py --check-reproducible` | 0 | reproducibility confirmed |
| `python scripts/render_eval_summary.py --check` | 0 | checked-in Eval summary is current |
| `git diff --check main...HEAD` | 0 | no whitespace errors |

## Fresh Python 3.11 verification

The host did not have Python 3.11 installed. The official Python.org installer
was downloaded from
`https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe` into a
GUID-named direct child of the Windows temporary directory before execution.

| Property | Verified value |
|---|---|
| Installer SHA-256 | `5ee42c4eee1e6b4464bb23722f90b45303f79442df63083f05322f1785f5fdde` |
| Authenticode status | `Valid` |
| Signer | `CN=Python Software Foundation, O=Python Software Foundation, L=Beaverton, S=Oregon, C=US` |
| Runtime | `Python 3.11.9` 64-bit |
| Installation | per-user temporary target; no launcher, PATH change, file association, or shortcut |

The temporary runtime created a new venv under the same GUID root. The
process-local `TEMP` and `TMP` values pointed to a child of that root so the
unmodified pytest command did not depend on a pre-existing shared pytest temp
directory. The editable installation and four repository gates then ran from
the repository root using the venv interpreter:

| Command | Exit | Result |
|---|---:|---|
| `python -m pip install -e ".[dev]"` | 0 | installed `clin-data-nav==0.3.0`, pytest 8.4.2, and PyYAML 6.0.3 |
| `python -m pytest -q` | 0 | `264 passed, 1 warning in 15.20s` |
| `python scripts/validate_skill.py` | 0 | no findings |
| `python scripts/check_public_boundary.py` | 0 | no findings |
| `python scripts/package_skill.py --check-reproducible` | 0 | reproducibility confirmed |

The warning was a `PytestCacheWarning`: the validation identity could not
write the repository's existing `.pytest_cache`. Test temporary files were
isolated to the GUID root, and no repository ACL, product behavior, or test
behavior was changed. The installer uninstall returned 0. Before deletion,
the resolved validation root was confirmed to be a GUID-named direct child of
the Windows temporary directory; that root was then removed and verified
absent.

## Eval evidence

`evals/cases.yaml` contains 12 cases. Filesystem-derived contract tests require
exact one-to-one catalog, baseline, forward, and generated-table coverage. All
12 baseline fixtures remained below the fixed threshold and failed; all 12
forward fixtures met or exceeded the threshold and passed.

| Case | Output depth | Baseline | Forward |
|---|---|---:|---:|
| `adam-quick-explanation` | quick explanation | 0 FAIL | 100 PASS |
| `teae-sas-spec` | implementation specification | 0 FAIL | 100 PASS |
| `sas-optimization-lexjansen` | evidence navigation | 10 FAIL | 210 PASS |
| `institutional-sql-without-dictionary` | implementation specification | 10 FAIL | 100 PASS |
| `stale-codingbook` | implementation specification | 20 FAIL | 100 PASS |
| `cdisc-variable-definition` | quick explanation | 10 FAIL | 100 PASS |
| `omop-phenotype` | implementation specification | 0 FAIL | 100 PASS |
| `tmucrd-public-profile` | evidence navigation | 30 FAIL | 100 PASS |
| `descriptive-rwd-no-tte` | research design | 0 FAIL | 140 PASS |
| `causal-rwd-tte-handoff` | research design | 10 FAIL | 200 PASS |
| `causal-rwd-incomplete-readiness` | research design | 0 FAIL | 150 PASS |
| `build-rwe-sap-unavailable` | implementation specification | 20 FAIL | 170 PASS |

These deterministic results verify repository response contracts only. They do
not prove semantic correctness, source accuracy, clinical validity, causal
validity, or complete real-world coverage.

## Release artifacts

`python scripts/package_skill.py` generated the following files, and
`python scripts/verify_release.py artifacts` independently verified the ZIP
against the manifest before the hashes were recorded.

| File | SHA-256 |
|---|---|
| `clinical-data-research-navigator-0.3.0.zip` | `7909B590CDEDCF707F1549355F8B1D3855F67A5FCF4C8FC066C1F322E84F85B1` |
| `clinical-data-research-navigator-0.3.0.manifest.json` | `7B9F085344780F3960380F7440023733879C1D8E89F5E871A7617F9D0B225639` |

The generated `dist` files are ignored build outputs and are not committed by
this evidence record.

## Complete diff and public-boundary audit

The complete `main...HEAD` diff was reviewed line by line: 59 files, 3,989
insertions, and 850 deletions. `git diff --check main...HEAD` passed. The audit
found:

- no unrelated or private institutional material;
- no private Adapter, codingbook, data dictionary, physical schema, linkage
  rule, PII classification, credential, or login-gated content;
- no deleted, skipped, or weakened baseline test;
- no rewrite of historical `0.2.2` changelog or Release records;
- no unpinned or mutable GitHub Action reference; and
- no tracked path returned by `git ls-files .baoyu-skills/**`.

The working tree was clean before the evidence report was created. The final
pre-commit rerun after this file's last content change passed the complete 264
test suite, Skill validation, public-boundary scan, reproducible-package check,
and `git diff --check`.

## External state not verified here

This is local evidence only. It does not verify or claim any of the following:

- a GitHub-hosted validation or Release workflow run for this commit;
- absence of Node.js deprecation annotations in a hosted run;
- branch protection or repository ruleset state;
- repository topics;
- private vulnerability reporting state after the dated repository
  observation;
- Dependabot security-update state;
- a pushed `main` commit or remote branch;
- an annotated `v0.3.0` tag; or
- a GitHub Release or publicly downloaded Release assets.

Those external actions and post-change re-reads remain subject to the
separately authorized publication sequence.
