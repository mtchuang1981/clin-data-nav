# v0.3.0 Post-Release Documentation Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make current repository guidance accurately describe the published `v0.3.0` release and preserve its verified public evidence without changing the immutable release.

**Architecture:** Treat documentation claims as tested repository contracts. Replace candidate-state assertions with published-state assertions, add one immutable dated evidence record, and extend the release procedure so future publication evidence cannot remain only in an execution transcript.

**Tech Stack:** Markdown, Python 3.11+, pytest, PyYAML, Git, GitHub CLI read-only evidence already captured during publication.

## Global Constraints

- Preserve the published `v0.3.0` annotated tag, GitHub Release, assets, package version, changelogs, and `CITATION.cff` unchanged.
- Preserve `docs/verification/2026-08-02-v0.3.0-local.md` as immutable historical candidate evidence.
- Update a failing test before each documentation behavior change.
- Use only verified public values; do not invent identity, DOI, UI assets, service levels, or enabled GitHub settings.
- Do not mutate branch protection, rulesets, topics, private vulnerability reporting, Dependabot, Zenodo, tags, Releases, or remote branches.
- Keep the tracked tree free of private institutional material and unrelated local configuration.
- Run the four repository gates plus Eval-summary and whitespace checks before completion.

---

## File responsibility map

- `tests/test_project_metadata.py`: executable contracts for published installation wording, supported versions, release-process evidence retention, and the dated public evidence report.
- `docs/installation.md`: English verified-Release installation instructions.
- `docs/installation.zh-TW.md`: aligned Traditional Chinese verified-Release installation instructions.
- `SECURITY.md`: current supported release line and dated private-reporting observation.
- `docs/release.md`: reusable post-publication evidence-retention requirement.
- `docs/verification/2026-08-09-v0.3.0-publication.md`: immutable public evidence for the exact v0.3.0 publication.

---

### Task 1: Replace candidate installation claims with published-release claims

**Files:**
- Modify: `tests/test_project_metadata.py:1190-1228`
- Modify: `docs/installation.md:69-78`
- Modify: `docs/installation.zh-TW.md:65-74`

**Interfaces:**
- Consumes: the existing bilingual installation-guide contract and version `0.3.0`.
- Produces: tested statements that `v0.3.0` is the current verified Release while retaining fail-closed ZIP/manifest verification.

- [ ] **Step 1: Change the installation contract first**

Replace the candidate claims in
`test_installation_guides_preserve_quick_update_verified_and_source_paths`
with published-state claims and explicit stale-language rejection:

```python
documents = (
    (
        (ROOT / "docs/installation.md").read_text(encoding="utf-8"),
        ENGLISH_ONBOARDING_CONTRACT,
        "current verified Release is `v0.3.0`",
        ("target version for the next release", "not yet a claim"),
    ),
    (
        (ROOT / "docs/installation.zh-TW.md").read_text(encoding="utf-8"),
        TRADITIONAL_CHINESE_ONBOARDING_CONTRACT,
        "目前已驗證的 Release 是 `v0.3.0`",
        ("下一個 Release", "不表示"),
    ),
)

for text, onboarding_contract, published_claim, stale_claims in documents:
    _assert_readme_onboarding_contract(text, **onboarding_contract)
    normalized = " ".join(text.split())
    assert published_claim in normalized
    for stale_claim in stale_claims:
        assert stale_claim not in normalized
```

Retain the existing assertions for version variables, SHA-256,
`archive_sha256`, source installation, no-overwrite behavior, and absence of
`v0.2.2`.

- [ ] **Step 2: Run the focused test and verify RED**

Run:

```text
python -m pytest -q tests/test_project_metadata.py -k installation_guides_preserve
```

Expected: FAIL because both guides still describe `v0.3.0` as unpublished.

- [ ] **Step 3: Make the minimum bilingual documentation change**

Use this English paragraph opening:

```markdown
The current verified Release is `v0.3.0`. Download both the ZIP and manifest
from that same Release, verify the ZIP's SHA-256 against `archive_sha256`, and
only then extract it. These examples refuse to overwrite an existing
destination.
```

Use this aligned Traditional Chinese opening:

```markdown
目前已驗證的 Release 是 `v0.3.0`。請從同一個 Release 下載 ZIP 與 manifest，
先用 `archive_sha256` 核對 ZIP 的 SHA-256，再進行解壓縮。下列範例遇到既有
目的目錄時會拒絕覆寫。
```

Do not change the PowerShell or POSIX verification commands.

- [ ] **Step 4: Run the focused test and verify GREEN**

Run the Step 2 command again.

Expected: PASS.

---

### Task 2: Advance the supported security line to 0.3.x

**Files:**
- Modify: `tests/test_project_metadata.py:1543-1563`
- Modify: `SECURITY.md:3-25`

**Interfaces:**
- Consumes: the policy that only the newest published release line receives fixes and the 2026-08-09 read-only GitHub observation.
- Produces: a tested `0.3.x` support table and truthful private-reporting fallback.

- [ ] **Step 1: Change the security contract first**

In `test_security_policy_has_supported_versions_and_safe_confidential_reporting`, replace the stale version/date assertions with:

```python
assert "`0.3.x` | Yes" in normalized
assert "`< 0.3` | No" in normalized
assert "`0.2.x` | Yes" not in normalized
assert "2026-08-09" in normalized
```

Keep every existing prohibited-material, coordination, best-effort, and
incident-response assertion.

- [ ] **Step 2: Run the focused test and verify RED**

Run:

```text
python -m pytest -q tests/test_project_metadata.py -k security_policy_has_supported_versions
```

Expected: FAIL because the policy still names `0.2.x` and 2026-08-02.

- [ ] **Step 3: Make the minimum policy update**

Set the supported-version introduction and table to:

```markdown
The newest published release line receives security fixes. As of 2026-08-09,
that line is `0.3.x`.

| Version | Supported |
|---|---|
| `0.3.x` | Yes |
| `< 0.3` | No |
```

Change the dated private-vulnerability-reporting observation to 2026-08-09,
retaining the verified disabled state and non-sensitive coordination route.

- [ ] **Step 4: Run the focused test and verify GREEN**

Run the Step 2 command again.

Expected: PASS.

---

### Task 3: Require and record immutable public publication evidence

**Files:**
- Modify: `tests/test_project_metadata.py`
- Modify: `docs/release.md:49-56`
- Create: `docs/verification/2026-08-09-v0.3.0-publication.md`

**Interfaces:**
- Consumes: exact public identities captured by the guarded publication and post-download verification.
- Produces: a reusable release-process requirement and one immutable v0.3.0 public evidence record.

- [ ] **Step 1: Add failing release-process and evidence-report tests**

Add these tests near the existing release and metadata contracts:

```python
def test_release_process_requires_a_committed_post_release_evidence_report():
    process = " ".join(
        (ROOT / "docs/release.md").read_text(encoding="utf-8").split()
    )
    assert "new dated verification report" in process
    assert "commit and push" in process
    assert "tag object" in process
    assert "asset IDs" in process
    assert "SHA-256" in process


def test_v030_publication_report_records_exact_public_evidence():
    report = (
        ROOT / "docs/verification/2026-08-09-v0.3.0-publication.md"
    ).read_text(encoding="utf-8")
    normalized = " ".join(report.split())

    required = (
        "6cf9593dd8a520f56e1e6e5b0bf2cb7d40b97791",
        "87ca8f379f6751fc465dbcd6ae8f430dabc73523",
        "31263843137",
        "31263961241",
        "https://github.com/mtchuang1981/clin-data-nav/releases/tag/v0.3.0",
        "506494592",
        "506494593",
        "18591",
        "1265",
        "ce6a67a268e4266d094db31406a9c5dda3f005c3b6a5355ec851ea87abf3aded",
        "d3862e00fcf499fa453e6cac05b6f3e21f9b2a9d735d2579a4a5b834caee42bc",
        "scripts/verify_release.py artifacts",
    )
    for value in required:
        assert value in normalized

    assert "branch protection: disabled" in normalized
    assert "rulesets: none" in normalized
    assert "topics: none" in normalized
    assert "private vulnerability reporting: disabled" in normalized
    assert "Dependabot security updates: disabled" in normalized
    assert "No external repository setting was changed" in normalized
```

- [ ] **Step 2: Run both new tests and verify RED**

Run:

```text
python -m pytest -q tests/test_project_metadata.py -k "committed_post_release or publication_report_records"
```

Expected: FAIL because the process lacks the retention step and the report
does not exist.

- [ ] **Step 3: Extend the release process**

Append publication step 7 to `docs/release.md`:

```markdown
7. Record the verified main commit, annotated tag object and peeled commit,
   validation and Release workflow URLs, public Release URL, asset IDs, sizes,
   and SHA-256 values in a new dated verification report under
   `docs/verification/`. Clearly separate verified public facts from external
   settings that remain disabled or unevaluated, then commit and push the
   report without moving the published tag or editing the Release.
```

- [ ] **Step 4: Write the dated publication report**

Create `docs/verification/2026-08-09-v0.3.0-publication.md` with these sections:

1. publication identity and immutable-boundary statement;
2. local Python 3.13 and official Python 3.11.9 verification summary;
3. hosted validation run and zero deprecation annotations;
4. annotated tag object and peeled commit;
5. guarded Release workflow and public Release URL;
6. asset table containing IDs, sizes, URLs, and both SHA-256 values;
7. independent download plus `verify_release.py artifacts` result;
8. dated external-setting re-read showing branch protection disabled,
   rulesets none, topics none, private reporting disabled, and Dependabot
   security updates disabled; and
9. remaining intentional boundaries: Zenodo, enriched identity, UI assets,
   Plugin product, and bundled `build-rwe-sap` remain absent.

State that no external repository setting was changed during the evidence
re-read. Do not copy or rewrite the older local report.

- [ ] **Step 5: Run both new tests and verify GREEN**

Run the Step 2 command again.

Expected: both PASS.

---

### Task 4: Verify the complete repair and commit it

**Files:**
- Review: every file modified in Tasks 1-3
- Preserve: all other tracked files

**Interfaces:**
- Consumes: the three green focused contracts.
- Produces: one reviewed post-release documentation repair commit on local `main`.

- [ ] **Step 1: Run the complete test suite**

Run:

```text
python -m pytest -q
```

Expected: 282 tests pass with no failures, skips, or xfails introduced.

- [ ] **Step 2: Run every required repository gate**

Run:

```text
python scripts/validate_skill.py
python scripts/check_public_boundary.py
python scripts/package_skill.py --check-reproducible
python scripts/render_eval_summary.py --check
git diff --check
```

Expected: every command exits 0 with no findings.

- [ ] **Step 3: Audit the exact diff and scope**

Run:

```text
git status --short
git diff --stat
git diff -- tests/test_project_metadata.py docs/installation.md docs/installation.zh-TW.md SECURITY.md docs/release.md docs/verification/2026-08-09-v0.3.0-publication.md
git diff --name-only
```

Verify that no historical verification record, version file, changelog,
packaged Skill file, workflow, tag, Release, or external setting changed.

- [ ] **Step 4: Commit the coherent repair**

Run:

```text
git add tests/test_project_metadata.py docs/installation.md docs/installation.zh-TW.md SECURITY.md docs/release.md docs/verification/2026-08-09-v0.3.0-publication.md
git diff --cached --check
git commit -m "docs: record v0.3.0 publication evidence"
```

- [ ] **Step 5: Verify the committed state**

Run:

```text
git status --short --branch
git show --stat --oneline HEAD
```

Expected: local `main` is clean and ahead of `origin/main`; no push occurs.
